{
  "name": "pgvector-embedding-google-gemma",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.0",
    "typescript": "^5.5.2",
    "vitest": "^4.0.16",
    "wrangler": "^4.55.0"
  }
}