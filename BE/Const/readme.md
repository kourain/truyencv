# Constant

## coding

tất cả mọi class, enum ở trong thư mục này đều sử dụng `namespace TruyenCV;`

## using

Khi sử dụng class, enum có trong thư mục này có thể gọi trực tiếp tên Enum hoặc Class, không gọi Const.[Tên Enum/Class]
